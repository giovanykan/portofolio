# Giovany Kantoro Design Portfolio

Interactive React portfolio converted from the original PDF portfolio.

## Run Locally

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

## Deploy

This project includes a GitHub Pages workflow. Push the repository to GitHub, enable Pages with `GitHub Actions` as the source, and each push to `main` will publish the built site.
